<h1>メモアプリ</h1>
<ul>
  <% @json_data.each do |memo_data| %>
    <li><a href='/memos/<%= memo_data['id'] %>'><%= h(memo_data['title']) %></a></li>
  <% end %>
</ul>
<div class='top_button'>
  <a href='/new'><button>作成</button></a>
</div>
